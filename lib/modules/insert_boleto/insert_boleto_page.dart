import 'package:flutter/material.dart';

class InserBoletoPage extends StatelessWidget {
  const InserBoletoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Insert Boleto Page'),
      ),
    );
  }
}
